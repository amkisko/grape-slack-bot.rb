Recorded 2026-09-07. Library gem. Grape 3.3 stores Endpoint::Options in @config. SlackBot already moved its config to @slack_bot_config after the 2.1.0 tag. Test lockfiles lagged the grape 3.3.5 registry release. Freshness, not a CVE. Gemspec already allows grape >= 1.6, < 4.0.

## Participants

- amkisko

## Decisions

- Cut 2.1.1 so the ivar rename ships as a versioned gem.
- Relock grape to 3.3.5 in Gemfile.lock and appraisal lockfiles.
- Add a live Grape request that calls production slack_bot_config and asserts Grape endpoint @config is not SlackBot::Config.
- Keep json on 2.21.2.

## Effects

- VERSION is 2.1.1. CHANGELOG.md has a 2.1.1 section for the config ivar.
- Root lockfile moved grape from 3.2.1 to 3.3.5 and dropped mustermann-grape. Appraisal lockfiles moved to 3.3.5 from 3.3.4, 3.3.2, or 3.3.3. Prism stayed on the prior pins so RuboCop on Ruby 4.0.2 still loads.

## Next

- Publish 2.1.1 when the branch is reviewed.

## Source

- usr/docs/changelogs/20260907163700_grape-3-3-5.md
- https://github.com/ruby-grape/grape/releases/tag/v3.3.5
