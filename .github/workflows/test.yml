name: test

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  audit:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - lock: Gemfile.lock
            ruby_version: "3.4"
          - lock: gemfiles/rails6.gemfile.lock
            ruby_version: "3.1"
          - lock: gemfiles/rails72.gemfile.lock
            ruby_version: "3.2"
          - lock: gemfiles/rails8ruby34.gemfile.lock
            ruby_version: "3.4"
          - lock: gemfiles/rails8ruby4.gemfile.lock
            ruby_version: "4.0"
            bundler_version: "2.7.2"
          - lock: gemfiles/rails8truffleruby.gemfile.lock
            ruby_version: truffleruby

    steps:
      - uses: actions/checkout@v6
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby_version }}
          bundler: ${{ matrix.bundler_version || 'default' }}
          bundler-cache: true
          cache-version: audit-${{ matrix.lock }}
      - name: Update advisory database
        if: matrix.lock == 'Gemfile.lock'
        run: bundle exec bundle audit check --update
      - name: Audit lockfile
        run: |
          lock="${{ matrix.lock }}"
          gemfile="${lock%.lock}"
          echo "==> ${gemfile}"
          BUNDLE_GEMFILE="${gemfile}" bundle install --quiet
          BUNDLE_GEMFILE="${gemfile}" bundle exec bundle audit check

  test:
    needs: audit
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          - gemfile: rails6
            ruby_version: "3.1"
          - gemfile: rails72
            ruby_version: "3.2"
          - gemfile: rails8ruby34
            ruby_version: "3.4"
          - gemfile: rails8ruby4
            ruby_version: "4.0"
            bundler_version: "2.7.2"
          - gemfile: rails8truffleruby
            ruby_version: "truffleruby"

    env:
      BUNDLE_GEMFILE: ${{ github.workspace }}/gemfiles/${{ matrix.gemfile }}.gemfile

    steps:
      - uses: actions/checkout@v6

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby_version }}
          bundler: ${{ matrix.bundler_version || 'default' }}
          bundler-cache: true

      - name: Run tests
        run: bundle exec polyrun parallel-rspec --workers 1 --merge-failures

  coverage:
    runs-on: ubuntu-latest
    env:
      BUNDLE_GEMFILE: ${{ github.workspace }}/gemfiles/rails8ruby34.gemfile
    steps:
      - uses: actions/checkout@v6
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.4"
          bundler-cache: true
      - run: POLYRUN_COVERAGE=1 bundle exec polyrun parallel-rspec --workers 1 --merge-failures
      - uses: codecov/codecov-action@v5
        continue-on-error: true
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
      - uses: codecov/codecov-action@v5
        if: ${{ !cancelled() }}
        continue-on-error: true
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          report_type: test_results
          files: coverage/junit-coverage.xml
